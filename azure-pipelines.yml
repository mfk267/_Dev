# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Feroze-PAYG(53013a13-85c5-48dd-a2b9-b7ae7a284380)'
    scriptType: 'pscore'
    scriptLocation: 'scriptPath'
    scriptPath: 'Create-NewRG.ps1'